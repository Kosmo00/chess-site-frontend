import React from 'react'

// react-bootstrap components
import Card from 'react-bootstrap/Card'

const SiteAds = () => {
  return (
    <Card>
      <Card.Body>
        <div id='ads-section'>
          Anuncios. Quitar este texto al llevar a produccion.
          A este se le pondra el id proporcionado por Google Adsense.
        </div>
      </Card.Body>
    </Card>
  )
}

export default SiteAds
